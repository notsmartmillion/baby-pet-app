export enum PetType {
  CAT = 'cat',
  DOG = 'dog',
}

export enum JobStatus {
  PENDING = 'pending',
  PROCESSING = 'processing',
  COMPLETED = 'completed',
  FAILED = 'failed',
}

export enum SubscriptionTier {
  FREE = 'free',
  MONTHLY = 'monthly',
  LIFETIME = 'lifetime',
}

